'use client'
import { useState } from 'react'
import { Header, Footer, PostCard } from '@/components/field-notes'
import { categories, posts } from '@/lib/field-notes'
export default function Archive() { const [query,setQuery]=useState(''); const [category,setCategory]=useState('All'); const filtered=posts.filter(p=>(category==='All'||p.category===category)&&`${p.title} ${p.excerpt}`.toLowerCase().includes(query.toLowerCase())); return <><Header/><main className="mx-auto max-w-6xl px-5 py-20"><p className="font-mono text-xs uppercase tracking-widest text-muted-foreground">The library</p><h1 className="mt-5 font-serif text-6xl tracking-tight">Archive</h1><p className="mt-5 max-w-xl text-lg leading-7 text-muted-foreground">Dispatches on attention, tools, practice, and the useful unknown.</p><div className="mt-14 flex flex-col gap-5 border-y border-border py-5 md:flex-row md:items-center md:justify-between"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search the archive" className="border-b border-border bg-transparent py-2 text-sm outline-none md:w-64"/><div className="flex flex-wrap gap-2">{categories.map(c=><button key={c} onClick={()=>setCategory(c)} className={`rounded-full border px-3 py-1.5 text-xs ${category===c?'bg-primary text-primary-foreground':'text-muted-foreground hover:text-foreground'}`}>{c}</button>)}</div></div><div className="mt-12 grid gap-x-8 gap-y-14 md:grid-cols-2">{filtered.map(p=><PostCard key={p.slug} post={p}/>)}{!filtered.length&&<p className="py-20 text-muted-foreground">No notes match that search.</p>}</div></main><Footer/></> }
