import Link from 'next/link'
import { ArrowLeft, Link2 } from 'lucide-react'
import { articleBody, getPost, posts } from '@/lib/field-notes'
import { Avatar, Footer, Header, Meta, PostCard } from '@/components/field-notes'
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=getPost(slug);return <><Header/><div className="fixed left-0 top-0 h-1 bg-primary" style={{width:'42%'}}/><main className="mx-auto max-w-6xl px-5 py-16"><Link href="/archive" className="flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/>Back to archive</Link><article className="mx-auto mt-16 max-w-3xl"><Meta post={post}/><h1 className="mt-6 font-serif text-5xl leading-[.98] tracking-tight md:text-7xl">{post.title}</h1><p className="mt-7 text-xl leading-8 text-muted-foreground">{post.excerpt}</p><div className="mt-8 flex items-center gap-3 border-y border-border py-5"><Avatar author={post.author}/><div className="text-sm"><div>{post.author.name}</div><div className="text-muted-foreground">{post.date} · {post.readTime}</div></div><button aria-label="Copy article link" className="ml-auto rounded-full border border-border p-2"><Link2 className="size-4"/></button></div><div className={`${post.color} mt-12 aspect-[2/1] rounded-sm`} /><div className="prose prose-neutral dark:prose-invert mt-14 max-w-none font-serif text-lg leading-8">{articleBody.map((block,i)=>block.type==='h2'?<h2 key={i} className="mt-12 text-3xl tracking-tight">{block.text}</h2>:block.type==='quote'?<blockquote key={i} className="my-10 border-l-2 border-primary pl-6 text-2xl italic leading-9">{block.text}</blockquote>:<p key={i}>{block.text}</p>)}</div></article><section className="mx-auto mt-24 max-w-6xl border-t border-border pt-12"><p className="font-mono text-xs uppercase tracking-widest text-muted-foreground">Keep reading</p><div className="mt-8 grid gap-8 md:grid-cols-2">{posts.filter(p=>p.slug!==post.slug).slice(0,2).map(p=><PostCard key={p.slug} post={p}/>)}</div></section></main><Footer/></>}
