import { getAuthor, posts } from '@/lib/field-notes'
import { Avatar, Footer, Header, PostCard } from '@/components/field-notes'
export default async function AuthorPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const author=getAuthor(slug);const written=posts.filter(p=>p.author.slug===author.slug);return <><Header/><main className="mx-auto max-w-6xl px-5 py-20"><div className="flex flex-col gap-7 border-b border-border pb-14 md:flex-row md:items-end"><Avatar author={author} large/><div><p className="font-mono text-xs uppercase tracking-widest text-muted-foreground">Contributor</p><h1 className="mt-3 font-serif text-6xl tracking-tight">{author.name}</h1><p className="mt-3 text-lg text-muted-foreground">{author.role} · {written.length} notes</p></div></div><p className="max-w-2xl py-12 text-2xl leading-10 text-muted-foreground">{author.bio}</p><div className="grid gap-12 border-t border-border pt-12 md:grid-cols-2">{written.map(p=><PostCard key={p.slug} post={p}/>)}</div></main><Footer/></>}
